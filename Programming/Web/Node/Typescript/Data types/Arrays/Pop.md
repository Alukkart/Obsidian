Удаляет последний элемент массива
# Синтаксис
```ts
Array<any>.pop(): any
```
# Возвращает
```ts
 Удаленный элемент
```

> [!Пример]
> ```ts
> let a:number[] = [1, 5]
> a.pop()
> console.log(a) //[1]
> ```
